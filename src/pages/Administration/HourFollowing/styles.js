import styled from "styled-components";

const HourFollowingContainer = styled.div`
  min-height: 100vh;

  .select {
    width: 250px;
  }

  .outlinedBox {
  }

  .divFooter{
    margin-top: 560px;
  }
`;

export { HourFollowingContainer };

